# Smoketest

smallest possible target — verifies the Examples package
builds against the parent Cast package. CI uses this as a no-model build gate.

## Source

Full source: [Examples/Sources/Smoketest/main.swift](https://github.com/jaylann/Cast/blob/main/Examples/Sources/Smoketest/main.swift)

```swift
// What this shows: smallest possible target — verifies the Examples package
// builds against the parent Cast package. CI uses this as a no-model build gate.

import Cast

print("ok")
```
